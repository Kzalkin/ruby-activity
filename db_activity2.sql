SELECT COUNT(id) AS total_number_of_students FROM students;

SELECT * FROM students WHERE location = 'Manila';

SELECT AVG(age) as average_age FROM students;

SELECT * FROM students ORDER BY age DESC;